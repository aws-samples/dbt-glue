name: 'test_s3_tables'
version: '1.0'
config-version: 2

profile: 'test'

model-paths: ["models"]
analysis-paths: ["analysis"]
test-paths: ["tests"]
seed-paths: ["data"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
    - "target"
    - "dbt_packages"

models:
  test_s3_tables:
    +materialized: table
    +file_format: iceberg

seeds:
  test_s3_tables:
    +file_format: iceberg
